<Window x:Class="BudgetApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BudgetApp.Views"
        xmlns:customControls="clr-namespace:BudgetApp.UserControls"
        mc:Ignorable="d"
        Title="واجهة تسجيل الدخول" Height="500" Width="678"
        WindowStartupLocation="CenterScreen"
        Closed="Window_Closed"
        Background="#fff">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="تسجيل الدخول" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="24" Foreground="Purple" FontWeight="Bold" Margin="0,0,0,15" />

            <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,15" >
                <Image Source="/Resourses/facebook.png" Width="32" />
                <Image Source="/Resourses/facebook.png" Width="32" />
                <Image Source="/Resourses/facebook.png" Width="32" />
            </StackPanel>

            <customControls:CustomTextBox Grid.Row="2"
                              WatermarkText="أدخل اسم المستخدم"
                              Margin="15" />

            <customControls:CustomTextBox Grid.Row="3"
                      WatermarkText="أدخل كلمة المرور"
                      Margin="15" />

            <customControls:CustomButton ButtonText="تسجيل الدخول" Grid.Row="4" Margin="5,15" Padding="10" FontSize="16" Height="60" Width="260" Click="login_Click" />


        </Grid>

        <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="335" Height="484" >
            <Grid.Background>
                <ImageBrush ImageSource="/Resourses/sign-background.jpg" Stretch="Fill"/>
            </Grid.Background>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="أليس لديك حساب؟ " VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" Foreground="White"  FontWeight="Bold" Margin="0,140,0,0" Grid.Row="2" />

            <customControls:CustomButton ButtonText="إنشاء حساب" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,230,10,0" Padding="10" FontSize="16" Grid.Row="2" Width="200" Height="60" Click="CustomButton1_Click" />
        </Grid>
    </Grid>
</Window>