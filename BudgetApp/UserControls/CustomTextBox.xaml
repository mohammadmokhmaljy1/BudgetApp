<UserControl x:Class="BudgetApp.UserControls.CustomTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="260"
             x:Name="customTextBox">

    <Grid>
        <Border x:Name="TextBoxBorder"
                BorderBrush="Purple"
                BorderThickness="1"
                CornerRadius="5"
                Background="White">

            <Grid>
                <TextBlock x:Name="WatermarkTextBlock"
                           Text="{Binding WatermarkText, ElementName=customTextBox}"
                           IsHitTestVisible="False"
                           VerticalAlignment="Center"
                           TextAlignment="Right"
                           HorizontalAlignment="Right"
                           Margin="5,0,0,0"
                           Padding="10"
                           Foreground="#808080"
                           FontStyle="Italic">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text.Length, ElementName=InputTextBox}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsFocused, ElementName=InputTextBox}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBox x:Name="InputTextBox"
                         Background="Transparent"
                         TextAlignment="Right"
                         BorderThickness="0"
                         Padding="10"
                         VerticalContentAlignment="Center"
                         Text="{Binding Text, ElementName=customTextBox, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>

        </Border>
    </Grid>
</UserControl>